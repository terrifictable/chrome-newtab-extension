# NewTab
Its a chrome extension, you need to press "load unpacked" to install it.


To make it show any bookmarks, rename `bookmarks.sample.js` in `startpage/` to `bookmarks.js` and customize them to your liking.